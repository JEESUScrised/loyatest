# Настройка Telegram Mini App через Vercel

## 🎯 Что это?
Инструкция по деплою фронтенда на Vercel для работы в Telegram Mini App. Vercel предоставляет бесплатный HTTPS URL без необходимости проброса портов.

## ✅ Преимущества Vercel:
- ✅ Бесплатно
- ✅ Автоматический HTTPS
- ✅ Простой деплой
- ✅ Автоматические обновления при push в GitHub
- ✅ Не нужен проброс портов
- ✅ Готовый публичный URL

## 🚀 Пошаговая настройка:

### Шаг 1: Подготовьте проект

Убедитесь, что фронтенд собирается:

```bash
cd frontends/client
npm run build
```

Если сборка успешна - можно деплоить!

### Шаг 2: Создайте аккаунт на Vercel

1. Перейдите на https://vercel.com
2. Нажмите "Sign Up"
3. Зарегистрируйтесь через GitHub (рекомендуется) или email
4. Подтвердите email (если регистрация через email)

### Шаг 3: Установите Git (если еще не установлен)

Если Git не установлен:

1. **Скачайте Git для Windows:**
   - Перейдите на https://git-scm.com/download/win
   - Скачайте и установите (оставьте настройки по умолчанию)

2. **После установки:**
   - Закройте и откройте PowerShell заново
   - Проверьте установку: `git --version`
   - Настройте Git (первый раз):
     ```bash
     git config --global user.name "Ваше Имя"
     git config --global user.email "your.email@example.com"
     ```

**Альтернатива:** Используйте GitHub Desktop (графический интерфейс):
- Скачайте с https://desktop.github.com/
- Установите и войдите в аккаунт GitHub

### Шаг 4: Подготовьте репозиторий на GitHub

Если код еще не на GitHub:

1. Создайте репозиторий на GitHub:
   - Перейдите на https://github.com/new
   - Введите имя репозитория (например: `loya`)
   - Выберите "Public" или "Private"
   - Нажмите "Create repository"

2. Загрузите код в репозиторий:

**Вариант A: Через командную строку (Git)**
```bash
# В корне проекта Loya
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/yourusername/loya.git
git push -u origin main
```

**Вариант B: Через GitHub Desktop**
1. Откройте GitHub Desktop
2. File → Add Local Repository
3. Выберите папку проекта `C:\Users\admnp\Desktop\Loya`
4. Нажмите "Publish repository"
5. Выберите репозиторий на GitHub

**Важно:** Замените `yourusername` на ваш GitHub username.

### Шаг 5: Подключите проект к Vercel

1. В Vercel нажмите **"Add New Project"**
2. Выберите ваш GitHub репозиторий
3. Если репозиторий не виден:
   - Нажмите "Adjust GitHub App Permissions"
   - Разрешите доступ к репозиторию
   - Обновите страницу

### Шаг 6: Настройте проект в Vercel

**Важно:** Для монорепозиториев рекомендуется использовать настройки напрямую в Vercel, а не `vercel.json`.

**Настройки проекта (рекомендуется):**

1. **Root Directory:** 
   - Нажмите "Edit"
   - Введите: `frontends/client`
   - Нажмите "Continue"
   - **Это самый важный шаг!** Vercel будет работать с этой папкой как с обычным React-проектом

2. **Framework Preset:** 
   - Должен автоматически определиться как "Create React App"
   - Если нет - выберите "Create React App" вручную

3. **Build Command:** 
   - Оставьте: `npm run build`
   - Или если пусто - введите: `npm run build`

4. **Output Directory:** 
   - Оставьте: `build`
   - Или если пусто - введите: `build`

5. **Install Command:** 
   - Оставьте: `npm install`
   - Или если пусто - введите: `npm install`

6. **Node.js Version (опционально, но рекомендуется):**
   - В настройках проекта Vercel → Settings → General
   - Найдите раздел "Node.js Version"
   - Установите: `18.x` или `20.x`
   - Или оставьте автоматическое определение (Vercel прочитает `.nvmrc` файл)

**Примечание:** Если в репозитории есть файл `vercel.json`, он может конфликтовать с этими настройками. В таком случае либо удалите его, либо переименуйте в `vercel.json.backup`.

### Шаг 7: Настройте переменные окружения

1. В разделе "Environment Variables" добавьте:
   - **Key:** `REACT_APP_API_URL`
   - **Value:** URL вашего бэкенда

   **Если бэкенд на localhost:**
   - Пока можно оставить пустым или указать `http://localhost:3000/api`
   - Позже задеплойте бэкенд отдельно (Railway, Render и т.д.)

   **Если бэкенд уже задеплоен:**
   - Укажите HTTPS URL бэкенда: `https://your-backend-url.com/api`

2. Нажмите "Add" для каждой переменной

### Шаг 8: Запустите деплой

1. Нажмите **"Deploy"**
2. Дождитесь завершения (обычно 1-2 минуты)
3. После успешного деплоя вы получите URL вида:
   - `https://loya-client.vercel.app`
   - Или `https://loya-client-xxxxx.vercel.app`

### Шаг 9: Обновите URL в настройках бота

1. Откройте Telegram
2. Найдите бота [@BotFather](https://t.me/BotFather)
3. Отправьте команду: `/myapps`
4. Выберите вашего бота: `testloyabot`
5. Выберите приложение: `testloya`
6. Выберите опцию: **"Edit Web App URL"**
7. Введите URL из Vercel: `https://loya-client.vercel.app`
   (или тот, который получили после деплоя)
8. Готово!

### Шаг 10: Проверьте работу

1. Откройте URL в браузере: `https://loya-client.vercel.app`
   - Должна открыться ваша страница
2. Откройте бота [@testloyabot](https://t.me/testloyabot) в Telegram
3. Нажмите кнопку **"Open"** или отправьте `/start`
4. Приложение должно открыться!

## 🔄 Автоматические обновления

После настройки:
- Каждый `git push` в GitHub → автоматический деплой на Vercel
- Vercel автоматически обновит приложение
- URL остается тем же

**Как обновить приложение:**
```bash
# Внесите изменения в код
git add .
git commit -m "Update app"
git push
# Vercel автоматически задеплоит обновления
```

## ⚙️ Настройка для продакшена

### Переменные окружения для продакшена:

В настройках проекта Vercel → Settings → Environment Variables:

```
REACT_APP_API_URL=https://your-backend-url.com/api
```

### Настройка кастомного домена (опционально):

1. Купите домен (например, на Namecheap, GoDaddy, Reg.ru)
2. В Vercel: Settings → Domains
3. Добавьте ваш домен
4. Настройте DNS записи (Vercel покажет инструкции):
   - Добавьте A-запись или CNAME
   - Vercel автоматически настроит SSL
5. Используйте кастомный домен в настройках бота

## 🔧 Деплой бэкенда (если нужно)

Если бэкенд тоже нужно задеплоить:

### Вариант A: Railway (рекомендуется)

1. Зарегистрируйтесь на https://railway.app
2. Нажмите "New Project"
3. Выберите "Deploy from GitHub repo"
4. Выберите ваш репозиторий
5. Выберите папку `backend`
6. Railway автоматически определит Node.js
7. В настройках проекта добавьте переменные окружения из `.env`:
   - `MONGODB_URI`
   - `JWT_SECRET`
   - `TELEGRAM_BOT_TOKEN`
   - И другие
8. Railway автоматически создаст HTTPS URL
9. Обновите `REACT_APP_API_URL` в Vercel на новый URL бэкенда

### Вариант B: Render

1. Зарегистрируйтесь на https://render.com
2. Нажмите "New +" → "Web Service"
3. Подключите GitHub репозиторий
4. Выберите папку `backend`
5. Настройте:
   - **Build Command:** `npm install`
   - **Start Command:** `npm start`
6. Добавьте переменные окружения
7. Нажмите "Create Web Service"
8. Получите HTTPS URL
9. Обновите `REACT_APP_API_URL` в Vercel

## 📋 Чек-лист настройки:

- [ ] Проект собирается локально (`cd frontends/client && npm install && npm run build`)
- [ ] Код загружен на GitHub
- [ ] Аккаунт Vercel создан
- [ ] Проект подключен к Vercel
- [ ] **Root Directory:** `frontends/client` ⚠️ **Важно!**
- [ ] **Framework Preset:** `Create React App`
- [ ] **Build Command:** `npm run build`
- [ ] **Output Directory:** `build`
- [ ] **Install Command:** `npm install`
- [ ] Файл `vercel.json` удален или переименован (если был)
- [ ] Переменные окружения настроены (`REACT_APP_API_URL`)
- [ ] Деплой успешен
- [ ] URL получен (например: `https://loya-client.vercel.app`)
- [ ] URL обновлен в настройках бота (@BotFather)
- [ ] Приложение открывается в браузере
- [ ] Приложение открывается в Telegram

## 🐛 Решение проблем

### Проблема: Ошибка 126 при деплое (`npm error code 126`)

**Причина:** Ошибка 126 обычно означает проблему с правами доступа или версией Node.js. Это может быть из-за:
- Неправильной версии Node.js
- Проблем с установкой зависимостей
- Проблем с правами доступа к `react-scripts`

**Решение:**

1. **Убедитесь, что в проекте есть `.nvmrc` файл:**
   - Файл `.nvmrc` уже создан в `frontends/client/.nvmrc` с версией `18`
   - Закоммитьте его:
     ```bash
     git add frontends/client/.nvmrc frontends/client/package.json
     git commit -m "Add Node.js version configuration"
     git push
     ```

2. **В настройках проекта Vercel:**
   - **Root Directory:** `frontends/client` ⚠️ **Это самое важное!**
   - **Framework Preset:** `Create React App`
   - **Build Command:** `npm run build` (теперь включает `CI=false`)
   - **Output Directory:** `build`
   - **Install Command:** `npm install`
   - **Node.js Version:** `18.x` (в Settings → General)

3. **Если в репозитории есть `vercel.json`, удалите или переименуйте его:**
   ```bash
   git mv vercel.json vercel.json.backup
   git commit -m "Disable vercel.json, use direct Vercel settings"
   git push
   ```

4. **Попробуйте очистить кеш в Vercel:**
   - Settings → General → Clear Build Cache
   - Запустите деплой заново

**Почему это работает:** 
- `.nvmrc` указывает Vercel использовать правильную версию Node.js
- `CI=false` в команде build отключает CI режим, который может вызывать проблемы
- Указав `frontends/client` как Root Directory, Vercel работает с этой папкой как с обычным React-проектом

**Решение 3 (если ничего не помогает):**
1. Убедитесь, что проект собирается локально:
   ```bash
   cd frontends/client
   npm install
   npm run build
   ```
2. Если локально не собирается - исправьте ошибки
3. Если локально собирается - попробуйте Решение 2

### Проблема: Ошибка "cd: frontends/client: No such file or directory"

**Причина:** Команда `cd` в `vercel.json` не может найти директорию. Это происходит потому, что Vercel выполняет команду в другом контексте.

**Решение:**
1. **Удалите или переименуйте `vercel.json`:**
   ```bash
   git mv vercel.json vercel.json.backup
   git commit -m "Remove vercel.json, use direct Vercel settings"
   git push
   ```

2. **В настройках проекта Vercel:**
   - **Root Directory:** `frontends/client` ⚠️ **Это самое важное!**
   - **Framework Preset:** `Create React App`
   - **Build Command:** `npm run build`
   - **Output Directory:** `build`
   - **Install Command:** `npm install`

3. Запустите деплой заново

### Проблема: Ошибка "Command exited with 1"

**Причина:** Команда сборки завершилась с ошибкой. Это может быть из-за:
- Ошибок в коде
- Проблем с зависимостями
- Проблем с конфигурацией

**Решение:**
1. Проверьте логи деплоя в Vercel - там будет указана точная ошибка
2. Проверьте, что проект собирается локально:
   ```bash
   cd frontends/client
   npm install
   npm run build
   ```
3. Если локально собирается, но на Vercel нет:
   - Убедитесь, что **Root Directory** установлен как `frontends/client`
   - Проверьте переменные окружения в Vercel
4. Если локально не собирается - исправьте ошибки компиляции

### Проблема: Деплой не удается

**Решение:**
- Проверьте, что проект собирается локально: `npm run build`
- Проверьте логи деплоя в Vercel
- Убедитесь, что Root Directory указан правильно

### Проблема: Приложение не открывается в Telegram

**Решение:**
- Проверьте, что URL правильно введен в настройках бота
- Проверьте, что URL начинается с `https://`
- Откройте URL напрямую в браузере - должно работать
- Проверьте консоль браузера на ошибки (F12)

### Проблема: API не работает

**Решение:**
- Проверьте переменную окружения `REACT_APP_API_URL` в Vercel
- Убедитесь, что бэкенд задеплоен и доступен
- Проверьте CORS настройки на бэкенде

## 🎉 Готово!

Теперь у вас есть:
- ✅ Публичный HTTPS URL
- ✅ Автоматические обновления
- ✅ Бесплатно
- ✅ Не нужен проброс портов
- ✅ Работает в Telegram Mini App

## 📞 Полезные ссылки:

- **Vercel:** https://vercel.com
- **GitHub:** https://github.com
- **Railway (для бэкенда):** https://railway.app
- **Render (для бэкенда):** https://render.com
- **BotFather:** https://t.me/BotFather

